{% extends 'base.html.twig' %}

{% block title %}Edit Offrir{% endblock %}

{% block body %}
    <h1>Modifier</h1>

    {{ include('offrir/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('offrir_index') }}">Retourner à la liste des rapports</a>

    {{ include('offrir/_delete_form.html.twig') }}
{% endblock %}
