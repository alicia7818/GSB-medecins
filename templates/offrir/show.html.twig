{% extends 'base.html.twig' %}

{% block title %}Offrir{% endblock %}

{% block body %}
    <h1>Offrir</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offrir.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ offrir.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offrir_index') }}">back to list</a>

    <a href="{{ path('offrir_edit', {'id': offrir.id}) }}">edit</a>

    {{ include('offrir/_delete_form.html.twig') }}
{% endblock %}
