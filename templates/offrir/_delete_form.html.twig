<form method="post" action="{{ path('offrir_delete', {'id': offrir.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offrir.id) }}">
    <button class="btn">Delete</button>
</form>
